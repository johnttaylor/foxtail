/** @namespace Fxt::Node

The 'Node' namespace is the root namespace for a Node (aka a Controller). In
general a Node is very concrete and is specific to a hardware platform.  A 
node is responsible for:

    - Defining the maximum number supported number of chassis
    - Supported:
        - point types
        - components
        - cards.

*/ 

  